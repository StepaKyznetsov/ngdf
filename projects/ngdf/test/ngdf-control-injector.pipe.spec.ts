import { NgdfControlInjectorPipe } from '../src/lib/pipes/ngdf-control-injector.pipe';

describe('NgdfControlInjectorPipe', () => {
  it('create an instance', () => {
    const pipe = new NgdfControlInjectorPipe();
    expect(pipe).toBeTruthy();
  });
});
